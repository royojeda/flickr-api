<h1>Flickr API</h1>

<%= form_with url: "/", method: :get do |form| %>
  <%= form.label :user_id, "User ID:" %>
  <%= form.text_field :user_id %>
  <%= form.submit "Search" %>
<% end %>

<% if @photos %>
  <%= @photos %>
<% end %>
